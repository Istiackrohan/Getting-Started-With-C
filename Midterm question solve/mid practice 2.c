/*#include<stdio.h>

int main(){
    int nums[12], i, sum = 0, maximum = 0, minimum = 0, multiplication;
    float average;

    for(int i = 0; i < 12; ++i) {
    scanf("%d", &nums[i]);
    if (nums[i] > maximum) maximum = nums[i];
    if (nums[i] < minimum) minimum = nums[i];
    sum = sum + nums[i];
    multiplication = multiplication * nums[i];
    }
    average = sum/12;
    printf("The Summation is: %d\n", sum);
    printf("The Maximum is: %d\n", maximum);
    printf("The Minimum is: %d\n", minimum);
    printf("The Average Number is: %f\n", average);
  return 0;
}*/
